<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abhishek.TargetDeals">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/targetico"
        android:label="@string/app_name"
        android:roundIcon="@drawable/targetico"
        android:supportsRtl="true">
        <activity
            android:name=".activity.SplashActivity"
            android:theme="@style/AppNoTitleBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.ImagePreviewActivity">
            >
        </activity>
        <activity
            android:name=".activity.DailogActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity
            android:name=".activity.ItemDetailsActivity"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>